/* eslint-disable no-unused-vars */
export default class Square {
  init(props){
    const {
      element,
      tableElement,
      mainRowElement,
      canvasElement,
      ctx,
    } = props;
    this.element = element;
  }
};
